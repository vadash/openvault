# Services Directory

This directory previously contained the ST Vector REST API service, which has been removed. All embedding storage is now local-only (Base64-encoded Float32Array stored in memory objects).
